# Supervised Fine-Tuning on the Hub

- 🐢 75 XP — LoRA adapter fine-tuned on a small instruction set with the adapter artifact uploaded to the Hub.
- 🐕 125 XP — Direct Preference Optimization (DPO) run on <=5k preference pairs with the adapter and reward curves published alongside the model card.
- 🦁 225 XP — Group Relative Policy Optimization (GRPO) run with a custom reward function, monitor stability, and publish policy + reward logs.

